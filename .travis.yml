language: php

php:
  - 7.0
  - 7.1
  - 7.2
  - 7.3
  - nightly

before_script:
  - travis_retry composer self-update
  - travis_retry composer update ${COMPOSER_FLAGS} --no-interaction --prefer-source

script:
  - phpunit --coverage-clover=coverage.xml --bootstrap vendor/autoload.php tests/TimecodeTest 

after_success:
  - bash <(curl -s https://codecov.io/bash)
